{
  "name": "ulm-spec",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "spec-md ./README.md > ./dist/index.html && cp ./*.png ./dist/",
    "dev-build": "(rm -r ./dist &>/dev/null || true) && mkdir ./dist && yarn run build",
    "now-build": "yarn run build",
    "test": "exit 0"
  },
  "devDependencies": {
    "spec-md": "^0.6.0"
  }
}
